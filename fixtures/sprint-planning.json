{
  "name": "sprint-planning",
  "description": "Sprint planning demo. User pastes 'meeting notes' via the suggestion chip; the agent (a) renders an animated image-attachment card via attachMeetingNotes (the handwritten Sprint 52 planning page), then (b) proposes the headline pull-in (ISS-113 GDPR) via propose_issue_change. Every fixture is gated on systemMessage 'project-management copilot' so the BFF's auto-thread-title call ('Generate a short title...') doesn't accidentally match the userMessage substring and double-trigger the demo.",
  "fixtures": [
    {
      "match": {
        "userMessage": "Plan the next sprint backlog",
        "systemMessage": "project-management copilot"
      },
      "chunkSize": 80,
      "response": {
        "toolCalls": [
          {
            "id": "call_attach_notes_001",
            "name": "attachMeetingNotes",
            "arguments": "{\"filename\":\"Sprint 52 Planning Notes.png\",\"size\":\"2.3 MB\",\"imageUrl\":\"/sprint-52.png\"}"
          }
        ]
      }
    },
    {
      "match": {
        "toolCallId": "call_attach_notes_001",
        "systemMessage": "project-management copilot"
      },
      "response": {
        "toolCalls": [
          {
            "id": "call_propose_iss113",
            "name": "propose_issue_change",
            "arguments": "{\"issue_id\":\"ISS-113\",\"changes\":{\"status\":\"Todo\"}}"
          }
        ]
      }
    },
    {
      "match": {
        "toolCallId": "call_propose_iss113",
        "systemMessage": "project-management copilot"
      },
      "response": {
        "content": "Read the notes. Pulling **ISS-113 (GDPR data export)** in as the Cycle 52 headline — Sarah lead, schema spike first. Approval card's in chat. Say 'what else should we pull in' and I'll layer in the search pagination fix and checkout sketch."
      }
    },
    {
      "match": {
        "userMessage": "what else should we pull in",
        "systemMessage": "project-management copilot"
      },
      "response": {
        "toolCalls": [
          {
            "id": "call_propose_iss111",
            "name": "propose_issue_change",
            "arguments": "{\"issue_id\":\"ISS-111\",\"changes\":{\"status\":\"Todo\"}}"
          }
        ]
      }
    },
    {
      "match": {
        "toolCallId": "call_propose_iss111",
        "systemMessage": "project-management copilot"
      },
      "response": {
        "content": "Queued **ISS-111 (search pagination)** next — half-day fix per the notes. After this, ISS-116 (checkout sketch) and the stretch ISS-105 priority bump are still open. Want to chain them?"
      }
    }
  ]
}
